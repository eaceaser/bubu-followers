{
  "type": "object",
  "properties": {
    "username": {
      "description": "Twitch.tv username",
      "type": "string",
      "default": ""
    },
    "pollInterval": {
      "description": "Interval to update followers (seconds)",
      "type": "integer",
      "default": 30,
      "minimum": 30
    }
  },
  "required": ["username"]
}
